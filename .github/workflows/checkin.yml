name: 起点读书自动签到

on:
  schedule:
    # 北京时间每天 10:11 运行 (UTC 2:11)
    - cron: '11 2 * * *'
  workflow_dispatch:

jobs:
  checkin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node qdreader.js
        env:
          QDREADER_COOKIE: ${{ secrets.QDREADER_COOKIE }}
          QDREADER_DEBUG: ${{ secrets.QDREADER_DEBUG }}
          # 通知配置 - 按需在 GitHub Secrets 中添加
          BARK_PUSH: ${{ secrets.BARK_PUSH }}
          BARK_SOUND: ${{ secrets.BARK_SOUND }}
          BARK_GROUP: ${{ secrets.BARK_GROUP }}
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_USER_ID: ${{ secrets.TG_USER_ID }}
          TG_API_HOST: ${{ secrets.TG_API_HOST }}
          PUSH_KEY: ${{ secrets.PUSH_KEY }}
          DD_BOT_TOKEN: ${{ secrets.DD_BOT_TOKEN }}
          DD_BOT_SECRET: ${{ secrets.DD_BOT_SECRET }}
          QYWX_KEY: ${{ secrets.QYWX_KEY }}
          PUSH_PLUS_TOKEN: ${{ secrets.PUSH_PLUS_TOKEN }}
          PUSH_PLUS_USER: ${{ secrets.PUSH_PLUS_USER }}
